/**
 * Connect form processing.
 *
 * @package Shorthand Connect
 */

document.getElementById( 'post' ).addEventListener(
	'submit',
	function () {
		document.getElementById( 'shorthand_update' ).disabled = true;
	}
);
